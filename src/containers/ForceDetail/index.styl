.force-detail-wrapper {
  position fixed;
  top: 200px;
  left: 630px;
  width: 310px;
  // height: 515px;
  padding: 15px;
  color: #fff;
  img {
    display: block;
    width: 100%;
    height: 155px;
  }
  .h6-name {
    font-size: 24px;
    opacity 0.8;
  }
  .info {
    font-size: 18px;
    opacity 0.8;
  }
  .timer-range-search {
    padding-top: 5px;
    .split-line {
      border: 1px solid #11517F;
    }
    .data-picker {
      display: block;
      margin: 10px 0;
    }
  }
  .action-detail-wrapper {
    // border: 1px solid red;
    display: flex;
    flex-wrap: wrap;
    justify-content space-between
    button {
      cursor pointer;
      margin-top: 14px;
      background: #20AAFF;
      border: none;
      outline: none;
      height: 35px;
      width: 47%;
      font-size: 22px;
      opacity 0.8;
      color: #FFF;
    }
  }
}
