.network-force-history-wrapper {
  position fixed;
  left: 108px;
  top: 96px;
  bottom: 80px;
   width: 506px;
 // width: 406px;
  color: #fff;
  display: flex;
  flex-direction: column;
  & > img{
    width: 10px;
  }
  .force-list {
    margin: 0;
    padding: 0 10px;
    list-style: none;
    position relative;
    flex: auto;
    overflow-x hidden;
    overflow-y auto;
    .active {
      background: #20AAFF;  
    }
    .force-item {
      display: flex;
      border-bottom: 1px solid #2C6575;
      padding: 10px;
      cursor pointer;
      & > img {
        width: 115px;
        height: 64px;
        flex: none;
        margin-right: 9px;
      }
      .force-desc {
        color: #fff;
        flex: auto;
        display: flex;
        flex-direction: column;
        margin-top: -5px;
        .name {
          font-size: 24px;
        }
        .item {
          font-size: 18px;
        }
      }
    }
  }
}
