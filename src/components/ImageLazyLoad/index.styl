.lazyload-img {
  // display: block;
  position: relative;
  // height: 100%;
  &::after {
    content: "加载中.." " " attr(alt);
    font-size: 16px;
    font-family: FontAwesome;
    // color: rgb(100, 100, 100);
    color: white;
    display: flex;
    justify-content: center;
    align-items: center;
    position: absolute;
    z-index: 2;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: #05121C;
  }
}